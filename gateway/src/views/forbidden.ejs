<%- include('partials/head', { pageTitle: 'Not authorized', active: '' }) %>

<div class="card">
  <h2>Not authorized</h2>
  <p class="hint" style="margin-bottom:0;">
    Your Access Role doesn't have permission to view <strong><%= area %></strong>. Ask an administrator
    to grant it on the <a href="/admin/roles">Access Roles</a> page, or head back to the
    <a href="/">Dashboard</a>.
  </p>
</div>

<%- include('partials/foot') %>
