{
  "key": "shelly-bulb",
  "label": "Shelly Bulb (RGBW, color mode)",
  "topicPrefixPattern": "^shellies/(shellybulb-[^/]+)/",
  "_comment": "Same shape as shelly-rgbw2.json, and for the same reason -- see that file's own comment. Not verified against a real Shelly Bulb device (unlike RGBW2, which was tested end-to-end), only against the documented API shape being identical to RGBW2's own color-mode one.",
  "supportsShellyRgbw": true,
  "commands": [
    {
      "key": "color",
      "label": "On/Off/Toggle",
      "topicTemplate": "shellies/{device}/color/{channel}/command",
      "actions": [
        "on",
        "off",
        "toggle"
      ]
    },
    {
      "key": "color-set",
      "label": "Set color (needs Shelly RGBW/White transform, mode rgb or rgb-percent)",
      "topicTemplate": "shellies/{device}/color/{channel}/set",
      "actions": [
        "rgb <hue 0-360>",
        "rgb <hue>,<saturation>,<value>",
        "rgb <red%+green%*1000+blue%*1000000>"
      ]
    },
    {
      "key": "white-set",
      "label": "Set white % (needs Shelly RGBW/White transform, mode white)",
      "topicTemplate": "shellies/{device}/color/{channel}/set",
      "actions": [
        "white <0-100>"
      ]
    }
  ]
}
